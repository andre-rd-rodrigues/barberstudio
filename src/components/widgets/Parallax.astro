---
interface Props {
  src?: string;
}

const { src } = Astro.props;
---

<div
  class={`relative bg-center bg-no-repeat h-96 flex flex-col justify-center items-center mt-24 parallax`}
  style={{ backgroundImage: `url('${src}')` }}
>
  <slot />
</div>
